# Base URL for the API (Express + Payload CMS). In dev this is proxied
# to http://localhost:3001 via vite.config.ts. Leave as /api in prod
# behind a reverse proxy.
VITE_API_BASE=/api

# Cloudinary cloud name used by lib/cloudinary.ts to build image URLs.
VITE_CLOUDINARY_CLOUD=bellagio
